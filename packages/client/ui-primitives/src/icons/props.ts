/** Shared sizing props for dsh brand artwork. */
export interface LogoProps {
  /** Requested logo width in pixels. */
  size?: number | undefined
  /** Optional class for layout and color. */
  className?: string | undefined
}
