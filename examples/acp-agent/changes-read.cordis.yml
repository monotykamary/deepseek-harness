# Isolated mutation-ledger scenario composition.
- id: base
  name: '@monotykamary/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: ui-deliverables
            name: '@monotykamary/dsh-client-ui-deliverables'
          - id: tool-session-mutations
            name: '@monotykamary/dsh-tool-session-mutations'
            config:
              maxListItems: 50
              maxDiffChars: 12000
