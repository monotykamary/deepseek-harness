# One-world invariant (same pairing as examples/headless-agent/e2b.cordis.yml):
# e2b.cwd and sandbox-policy.workspaceRoot must name the same remote directory,
# which is also bash-local's implicit default workdir.
- id: e2b
  name: '@monotykamary/dsh-e2b'
  config:
    cwd: !!js process.cwd()
    timeoutMs: 180000

- id: subprocess-e2b
  name: '@monotykamary/dsh-subprocess-e2b'

- id: bash
  name: '@monotykamary/dsh-bash-local'
  config:
    timeoutMs: 30000

- id: fs-e2b
  name: '@monotykamary/dsh-fs-e2b'

- id: agents
  name: '@monotykamary/dsh-agent'

- id: sandbox-policy
  name: '@monotykamary/dsh-sandbox-policy'
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: pty
  name: '@monotykamary/dsh-terminal'

- id: terminal-bash
  name: '@monotykamary/dsh-terminal-bash'
  config:
    pollIntervalMs: 25
    exactProbeAfterMs: 150
    idleSilenceMs: 2000
    handoffGraceMs: 500
    timeoutMs: 5000
    disposeGraceMs: 1000

- id: lsp
  name: '@monotykamary/dsh-lsp'

- id: lsp-stdio
  name: '@monotykamary/dsh-lsp-stdio'
  config:
    servers:
      fixture:
        command: node
        args:
          - !!js process.cwd() + '/fixture-lsp.mjs'
        extensionToLanguage:
          .ts: typescript
        shutdownTimeoutMs: 1000
        killGraceMs: 500
