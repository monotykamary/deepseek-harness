# Add the persisted goal domain and its model-facing tools to the real one-shot app.
- id: base
  name: '@monotykamary/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: goal
            name: '@monotykamary/dsh-goal'
          - id: tool-goal
            name: '@monotykamary/dsh-tool-goal'
